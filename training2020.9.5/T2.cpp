#include <algorithm>
#include <iostream>
#include <cstring>
#include <cstdio>
#include <cmath>

using namespace std;

const int maxn = 3e5 + 5;

int n, id[maxn];
string str[maxn];
int main() {
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    
    return 0;
}