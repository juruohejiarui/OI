#include <iostream>
#include <cstring>
#include <cstdio>
#include <cmath>

using namespace std;

const int maxn = 1e6 + 5;
struct PAMNode {
    int fail, len, cnt;
} nd[maxn];

char str[maxn];
void PAM_getfail(int nd, int pos) {
    
}
int main() {
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    return 0;
}